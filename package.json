{
  "name": "vapebay",
  "private": true,
  "description": "VapeBay monorepo — runs all three services with `npm run dev`",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:api": "cd backend-api && npx ts-node -r dotenv/config src/main.ts",
    "dev:admin": "cd backend-admin && php artisan serve --host=127.0.0.1 --port=8000",
    "dev:web": "cd frontend && npm run dev",
    "seed": "cd backend-api && npx ts-node -r dotenv/config src/seed.ts",
    "test": "cd backend-api && npm test",
    "test:e2e": "cd backend-api && npm run test:e2e",
    "test:admin": "cd backend-admin && php artisan test"
  },
  "devDependencies": {
    "cross-spawn": "^7.0.6",
    "tree-kill": "^1.2.2"
  }
}
